# LiteNote: Python-SQLite CRUD System

A lightweight, console-based note-taking application that demonstrates **database persistence** and **backend logic**.

## 🚀 Features
* **Persistent Storage:** Uses SQLite to save data to a local `.db` file.
* **Full CRUD Lifecycle:** Users can Create, Read, and View notes through an interactive menu.
* **Relational Mapping:** Implements a structured SQL schema with auto-incrementing IDs.

## 🛠️ Tech Stack
* **Language:** Python 3.x
* **Database Engine:** SQLite3
* **Tools:** DB Browser for SQLite (for verification)

## 📖 How to Run
1. Ensure you have Python installed.
2. Download `app.py` and `notes.db`.
3. Open your terminal in the project folder and run:
   ```bash
   python app.py
